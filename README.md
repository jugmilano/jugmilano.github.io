JUG Milano website
======

This is the website of JVM User Group Milano.

If `Gemfile` is missing,

```
bundle init
bundle add jekyll
```

To live preview:

```
bundle update
bundle exec jekyll serve
```
